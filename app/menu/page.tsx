// ============================================================
// Menu Page
// Wraps the MenuBuilder component (step-by-step tortelloni builder)
// ============================================================

import { MenuBuilder } from '@/components/menu-builder'

export default function MenuPage() {
  return <MenuBuilder />
}
